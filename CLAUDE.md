<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan:
`.specify/specs/006-tasks-capture-inbox/plan.md`
<!-- SPECKIT END -->

# Second Brain

Personal AI assistant: voice/text dump → structured tasks → goal-aligned planning → evening reflection.

## Стек
- **Backend:** Python 3.12, FastAPI + Pydantic V2, PyJWT (Supabase JWT), slowapi, Sentry
- **Mobile:** Expo SDK 53, Expo Router v4, TypeScript strict, Zustand + MMKV, expo-audio (NOT expo-av), @sentry/react-native, expo-notifications, react-native-purchases, expo-apple-authentication, @react-native-google-signin/google-signin
- **DB:** Supabase Postgres + RLS + `pgvector halfvec(1536)` + HNSW
- **AI router (cheapest-first):** Groq Llama 3.3 70B → Claude Haiku 4.5 → Claude Sonnet 4.6
- **STT:** gpt-4o-mini-transcribe → HuggingFace Whisper (fallback через `router.huggingface.co/hf-inference/models/<HUGGINGFACE_STT_MODEL>`, по умолчанию `openai/whisper-large-v3`); при сбое STT `POST /dump/audio` отдаёт 503
- **Premium:** RevenueCat → backend webhook → `user_premium` table
- **Deploy:** Railway (Dockerfile) для backend, EAS для mobile

## Структура
- `second-brain/backend/` — FastAPI app (api/, services/, models/, tests/)
- `second-brain/mobile/` — Expo app (app/ Router screens, services/, store/, components/)
- `second-brain/supabase/migrations/` — SQL миграции (001–023; 015–017 — Tasks Stage 03, 018 — Goals OKR/Strategy/KPI/Weekly Review, 019–022 — Health/Sleep/Nutrition, 023 — Workouts/Training feature 009: `health_exercises`, `health_workout_sessions/sets/supersets`, `health_workout_programs/program_sessions/program_exercises`, `health_workout_one_rm_history`, `health_workout_personal_records`, `health_user_training_profile`; RLS + trigger `legacy_workout_mirror` зеркалит completed sessions в legacy `health_workouts` для backwards-compat)
- `second-brain/docs/` — setup guides (`oauth-setup.md`, `revenuecat-setup.md`)
- `second-brain/mobile/locales/` — i18n resources (`ru.json`, `en.json`)
- `second-brain/mobile/plugins/withPrivacyManifest.js` — Expo config plugin, копирует `ios/PrivacyInfo.xcprivacy.template.plist` в билд
- `second-brain/telegram-miniapp/src/screens/finance/` — Hi-fi редизайн раздела Финансы (12 экранов: Overview, Transactions, Budgets, Goals, NetWorth, Assets, Income, Subscriptions, Debts, Taxes, AI, Analytics + More-хаб). Дизайн-система в `finance.css` под скоупом `.finance-app` (Manrope + JetBrains Mono, красный hero, кремовый фон, pill-кнопки). Роуты `/finance/*` в `src/app/routes.tsx`; общий shell в `components/shell.tsx` (FinancePhone, FinanceTabBar, Pill, Alert, Segmented, Skeleton, EmptyState, ErrorState), иконки в `components/Icon.tsx`. Все экраны подключены к API через `@tanstack/react-query`. Формы создания (10 sheet-форм) в `components/forms.tsx` + общий `Sheet` в `components/Sheet.tsx` — открываются с FAB/кнопок на каждом экране, пишут через `createFinance*` и инвалидируют `["finance"]` query keys.
- Backend Finance API: `second-brain/backend/api/finance.py` (роутер `/finance`, подключён в `main.py`) + `models/finance.py` + `services/finance_analyzer.py`. Миграция `second-brain/supabase/migrations/014_finance.sql`. Тесты — `backend/tests/test_finance_api.py`.
- Onboarding state (mini-app): `second-brain/telegram-miniapp/src/services/onboarding.ts` хранит локальный флаг прогресса; `routeAfterTelegramSession` ведёт в `/onboarding/setup`, если `is_new_user || !user.is_onboarded || !isOnboardingComplete()`.
- Launch splash (mini-app): `second-brain/telegram-miniapp/src/screens/launch/LaunchScreen.tsx` + SVG-логотип `src/assets/AppLogo.tsx` + блок стилей `.launch-splash` в `src/styles.css`. Тёмный фон с радиальным свечением, бренд `AI ASSISTANT` (Manrope 800), pill-статус со спиннером (`launch.statusConnecting` пока идёт `createTelegramSession` / `createDevSession`); после bootstrap — статусный текст и кнопки fallback. Тест: `tests/launch.test.tsx`.
- Tasks Capture + Inbox (Phase 1, spec `.specify/specs/006-tasks-capture-inbox/`): миграция `015_tasks_inbox.sql` (status enum inbox|active|done|archived|delegated, raw_text column, partial indices), новые endpoint'ы `POST /tasks`, `GET /tasks/inbox`, `POST /tasks/{id}/process` в `api/tasks.py`. Дамп через `/dump` теперь складывает задачи со `status='inbox'` и `raw_text` оригинала. Расширенный NLP-парсер в `services/task_parser.py` (поверх `ai_router`, fallback на `services/parser.py:_fallback_parse_dump`) извлекает time_of_day, duration_estimated_min, contact, url, clarification_questions. Mini-app: quick-add input в TasksScreen + новый InboxScreen (`/tasks/inbox`) с 4 process actions, расширенный TaskDetailScreen (raw_text + status badge). Sentry `before_send` фильтрует `raw_text`/`source_text` глобально (privacy FR-SEC-002). Тесты: `backend/tests/test_tasks_capture.py`, `test_task_parser_v2.py`, `test_privacy_raw_text.py`, `test_migration_015_backfill.py`; `miniapp/tests/tasks-capture.test.tsx`, `inbox.test.tsx`.
- Goals Stage 02 (OKR + Strategy + KPI + Weekly Review): миграция `018_goals_okr_strategy_kpi.sql` расширяет `goals` (level enum `life|year|quarter|week`, `parent_goal_id`, `horizon_start/end`, `weight 1-10`, + sphere включает `mind`/`personal`) и добавляет таблицы `goal_key_results`, `user_strategy`, `user_kpis`, `user_kpi_history`, `weekly_reviews` (все с RLS `auth.uid()=user_id` и триггерами `updated_at`). Backend: `api/goals.py` (расширен `GET /goals/tree`, KR CRUD на `/goals/{id}/key-results`, фильтры `level`/`parent_goal_id`), новые роутеры `api/strategy.py` (`GET/PUT /strategy`), `api/kpis.py` (`/kpis` CRUD + `/kpis/{id}/history`), `api/reviews.py` (`/reviews/weekly/draft`, `/reviews/weekly` upsert по `user_id+week_start`). Модели: `models/goal.py` (Goal/GoalCreate/Update + KeyResult*), `models/strategy.py`, `models/kpi.py`, `models/weekly_review.py`. Сервисы: `services/okr_progress.py` (`compute_kr_progress` с поддержкой increase/decrease/maintain, `derive_kr_status` с дедлайн-логикой, `aggregate_goal_progress` приоритет KR>tasks>manual, `build_okr_tree`), `services/kpi_analytics.py` (trend %, status ok/warning/breach), `services/weekly_review_builder.py` (ISO week, draft с suggestions). Тесты: `test_okr_progress.py`, `test_kpi_analytics.py`, `test_weekly_review_builder.py`, `test_goals_okr_api.py`, `test_strategy_api.py`, `test_kpis_api.py`, `test_reviews_api.py`. Mini-app: дизайн-система в `screens/goals/goals.css` под скоупом `.goals-app` (Manrope, лавандовый accent `#6E5BF6`, ink `#1A1340`, screen-bg `#F3F1FC`), общий shell `screens/goals/components/shell.tsx` (GoalsApp, GoalsScreenLayout, GoalsTopBar, GoalsBody, GoalsTabBar, levelLabel/levelColor/statusLabel). 7 hi-fi экранов с роутами `/goals/*` в `app/routes.tsx`: `/goals` (hero KPI + segmented life/year/quarter/week + OKR-дерево с fallback из flat goals), `/goals/new` (форма с level/parent/weight/sphere), `/goals/:id` (hero + edit + KR CRUD inline + linked tasks + delete confirm), `/goals/strategy` (mission/vision + chips для values/life_areas/SWOT 4×), `/goals/kpi` (KPI cards со sparkline, trend %, статус, inline-запись значения), `/goals/review` (weekly draft + форма highlights/lessons/next + mood/energy 1-5 + история), `/goals/more` (tile-хаб). Types — `types/api.ts` расширен `GoalLevel`, `KeyResult*`, `GoalTreeNode`, `Strategy`, `Kpi*`, `WeeklyReview*`. API клиент — `services/api.ts` дополнен `getGoalTree`, `listKeyResults/createKeyResult/updateKeyResult/deleteKeyResult`, `getStrategy/updateStrategy`, `listKpis/createKpi/updateKpi/deleteKpi/addKpiHistoryEntry`, `getWeeklyDraft/listWeeklyReviews/upsertWeeklyReview`. Тесты `tests/goals.test.tsx` обновлены (level/weight в моках).
- Home + AppShell Hi-fi редизайн (mini-app, navy + ice white + sphere-палитра): главная `/today` ([TodayScreen.tsx](second-brain/telegram-miniapp/src/screens/today/TodayScreen.tsx)) переписана под скоупом `.home-app` — header (avatar + greet + search/bell), hero «Задачи» (синий пастель, цифра дня, кольцо прогресса, 4 KPI Big3/Inbox/Готово/Всего из `getTodayTasks` + `getInboxTasks`), 2×2 сетка разделов в своих цветах (Финансы красный/`getFinanceDashboard`, Здоровье мятный/моки, Цели лавандовый/`listGoals`, Дневник персиковый), горизонтальная лента AI-рекомендаций (Inbox/Big3/бюджет/«Записать мысль»), список задач сегодня. Hero — `<section role="link">` + `useNavigate` чтобы не вкладывать `<a>` в `<a>`. AppShell ([AppShell.tsx](second-brain/telegram-miniapp/src/components/AppShell.tsx)) — bottom-sheet `.app-menu-sheet` «Second Brain»: drag handle, лого SB + бейдж «N разделов», поиск с live-фильтром и ⌘K, цветная 2×N сетка (active — заливка цветом раздела с тенью), footer-AI-подсказка. Сохранены контракт-точки тестов: `aria-label="Открыть меню"` / `"Разделы приложения"` / класс `active` на ссылке. Стили — блок `.home-app` / `.app-menu-sheet` / `.app-menu-trigger` в `src/styles.css`. Тесты: `tests/today.test.tsx`, `tests/app-shell.test.tsx` (зелёные).
- Tasks Stage 03 backend (полный GTD-стек поверх Capture+Inbox): миграции `016_tasks_stage03.sql` (subtasks/dependencies, eisenhower_quadrant, big_three_for_date, time_block_start/end + estimate_min/actual_min + deep_work, contexts/tags, saved_filters, comments, attachments) и `017_tasks_stage03_completion.sql` (project_templates, recurrence rule jsonb + last_completed_at + chain_count, habit_chains, focus_sessions + focus_settings, task_ai_logs). Новые сервисы `services/task_planning.py` (Eisenhower-квадрант, Big Three по дате, time-blocks CRUD + capacity/free-slots), `services/task_focus.py` (Pomodoro sessions + summary + settings), `services/task_filters.py` (search/saved filters AND/OR), `services/task_relations.py` (dependencies + comments + attachments), `services/task_ai_planning.py` (suggest/split/plan-day/deadline-risk/process-inbox через `ai_router`), `services/task_analytics.py` (analytics, weekly_report, estimate_vs_actual, by_weekday, rollover_patterns, project_analytics), `services/task_recurrence.py` (rollover + don't-break-the-chain + habit_stats), `services/task_projects.py` (PARA: projects/templates/from-template, checklist CRUD, project_progress). `api/tasks.py` обслуживает /tasks/today|inbox|search|matrix|big-three|time-blocks (+capacity/free-slots), /tasks/{id}/focus-sessions + /focus-summary + /focus-settings, /tasks/{id}/checklist, /tasks/filters (+ /apply), /tasks/analytics/* + /weekly-report, /tasks/habits + /habits/{id}/stats, /tasks/recurrence/run-due + /{id}/recurrence + /{id}/rollover, /tasks/{id}/dependencies, /tasks/{id}/comments, /tasks/{id}/attachments, /tasks/ai/{suggest|split|plan-day|deadline-risk|process-inbox}, /tasks/capture/{telegram|voice|browser|email|import}. `api/task_projects.py` обслуживает /task-projects (+ templates + from-template + archive). Тесты: `test_tasks_stage03_completion_api.py`, `test_migration_017_tasks_stage03_completion.py` (плюс существующие capture/inbox/parser/privacy/migration_015).
- Tasks Hi-fi редизайн (mini-app, navy + ice white): дизайн-система в `second-brain/telegram-miniapp/src/screens/tasks/tasks.css` под скоупом `.tasks-app` (Manrope, accent `#2E5BFF`, navy ink `#0B1F3E`, screen-bg `#F2F6FC`). Общий shell в `screens/tasks/components/shell.tsx` (TasksApp, Screen, ScreenBody, TopBar, BackBtn, IconBtn, AIChip, SphereChip, TaskRow, TabBar, DateStrip) + 36 иконок в `components/Icon.tsx`. Локальный `TaskSphere = work|health|finance|personal|mind` мапится из backend `Sphere` в [TasksScreen.tsx:26](second-brain/telegram-miniapp/src/screens/tasks/TasksScreen.tsx#L26). 12 экранов с роутами `/tasks/*` в `app/routes.tsx`: `/tasks` (Today + Big Three + Inbox bar + quick-add), `/tasks/inbox`, `/tasks/:taskId`, `/tasks/more` (хаб), `/tasks/ai` (chat), `/tasks/matrix` (Эйзенхауэр), `/tasks/big-three`, `/tasks/timeblock` (timeline + now-line), `/tasks/focus` (Pomodoro 25/5), `/tasks/projects` (PARA), `/tasks/habits` (don't-break-the-chain), `/tasks/contexts` (GTD), `/tasks/analytics` (карма + estimate-vs-reality). Tab `Фокус` ведёт на `/tasks/focus`. Existing tests (`tests/tasks.test.tsx`, `tests/inbox.test.tsx`) обновлены — sphere segmented filter и Save/Done/Delete кнопки сохранены для тестируемости.
- Глобальная навигация (mini-app): гамбургер-меню удалено. `AppShell` ([components/AppShell.tsx](second-brain/telegram-miniapp/src/components/AppShell.tsx)) рендерит контент + `<AppTabBar>` снизу + фиксированную круглую кнопку `<ProfileButton>` в верхнем правом углу (скрывается на `/profile`). Проп `hideTabBar` (используется в `/onboarding/*`) убирает обе. `<AppTabBar>` ([components/AppTabBar.tsx](second-brain/telegram-miniapp/src/components/AppTabBar.tsx)) — 5 вкладок: **Дом** (`/today`) / **Задачи** (`/tasks`, активна и на `/goals/*`) / **+** / **Финансы** (`/finance`) / **Здоровье** (`/health`). Центральная кнопка `+` открывает `<AiInputSheet>` ([components/AiInputSheet.tsx](second-brain/telegram-miniapp/src/components/AiInputSheet.tsx)) — bottom-sheet с textarea + Фото (camera input) + Файл + Голос (MediaRecorder) + Отправить. Текст → `POST /dump/text`, голос → `POST /dump/voice`, фото/изображение → новый `POST /dump/photo` (через GPT-4o-mini Vision OCR, [services/vision_ocr.py](second-brain/backend/services/vision_ocr.py) + `MAX_PHOTO_SIZE=10MB`). Цели доступны как плитка внутри `/tasks/more`. Все scoped-хедеры (`home-app__head`, `red-head`, `tasks-app .top-bar`, `g-topbar`, `health-header`) имеют `padding-right: 56-64px` для зарезервированного слота под кнопкой профиля. Стили `.app-tabbar`, `.app-tabbar__plus`, `.ai-sheet*`, `.app-profile-btn` в [styles.css](second-brain/telegram-miniapp/src/styles.css). Тесты: [tests/app-shell.test.tsx](second-brain/telegram-miniapp/tests/app-shell.test.tsx) — 7 кейсов (вкладки, активность по path, `+` открывает sheet, hideTabBar скрывает таб-бар и кнопку профиля). Шрифты Inter Tight + Manrope + JetBrains Mono подключены в [index.html](second-brain/telegram-miniapp/index.html).
- Workouts/Training feature 009 — Phase 1+2 (schema + library + sessions/sets CRUD): миграция [023_workouts.sql](second-brain/supabase/migrations/023_workouts.sql) (10 таблиц + RLS + trigger `legacy_workout_mirror` зеркалит completed sessions в `health_workouts`); seed-файл [exercises_seed.json](second-brain/backend/data/exercises_seed.json) — 157 системных упражнений RU+EN. Backend: модели [models/workout.py](second-brain/backend/models/workout.py) (`Exercise`, `WorkoutSession`, `WorkoutSet`, `Superset` + Create/Update с validators), сервисы [workout_exercise_library.py](second-brain/backend/services/workout_exercise_library.py) (`seed_if_empty()` на startup в [main.py](second-brain/backend/main.py) lifespan, `list_exercises`, `get_exercise`, `popular_for_user`) и [workout_logger.py](second-brain/backend/services/workout_logger.py) (CRUD sessions/sets/supersets + `finish_session` с TRIMP-load + intensity minutes WHO + автозамыкание длительности из started_at). Роутеры [api/workouts.py](second-brain/backend/api/workouts.py) и [api/exercises.py](second-brain/backend/api/exercises.py) подключены в main.py — endpoints `/workouts/sessions` (CRUD + `/start` + `/finish` + `/active`), `/workouts/sessions/{id}/sets` (POST/GET), `/workouts/sets/{id}` (PATCH/DELETE), `/workouts/sessions/{id}/supersets`, `/exercises` (filter muscle/equipment/category/sport_kind/search), `/exercises/{slug}`, `/exercises/popular`. Без GPS в v1 — outdoor поля (distance_km/pace/elevation/swolf/laps) прямо в session. Тесты: [test_workout_logger.py](second-brain/backend/tests/test_workout_logger.py) (compute_training_load, intensity_minutes, session_volume_kg), [test_workout_models.py](second-brain/backend/tests/test_workout_models.py) (Pydantic validation), [test_workouts_api.py](second-brain/backend/tests/test_workouts_api.py) (CRUD endpoints с mocked supabase). Mini-app: типы в [types/api.ts](second-brain/telegram-miniapp/src/types/api.ts) (`SportKind`, `Exercise`, `WorkoutSession`, `WorkoutSet`, `Superset` + Create/Update), API клиент [services/api.ts](second-brain/telegram-miniapp/src/services/api.ts) (~15 helpers: `listExercises/getExercise/getPopularExercises`, `listWorkoutSessions/getActiveWorkoutSession/getWorkoutSession/createWorkoutSession/updateWorkoutSession/deleteWorkoutSession/startWorkoutSession/finishWorkoutSession`, `listWorkoutSets/createWorkoutSet/updateWorkoutSet/deleteWorkoutSet`, `listSupersets/createSuperset`). Zustand store [useActiveWorkoutStore.ts](second-brain/telegram-miniapp/src/store/useActiveWorkoutStore.ts) с persistence в localStorage для активной сессии + rest timer (выживает навигацию). Дизайн-система Workouts под скоупом `.health-app`: расширение [health.css](second-brain/telegram-miniapp/src/screens/health/health.css) (workout-card с per-sport accent-bar, set-row inline-edit grid, timer-big, sport-chip, health-sheet, sport-picker tiles, filter-chips, exercise-list-row), компоненты в `screens/health/components/` — `shell.tsx` (HealthApp/HealthScreenLayout/HealthTopBar/HealthBackButton/HealthIconButton/HealthCta/SectionTitle/EmptyState/HealthBody), `Icon.tsx` (33 иконки: dumbbell/barbell/kettlebell + running/cycling/swimming/skiing/climbing/rowing/golf/yoga + heart-pulse/stopwatch/mountain/target/trophy/flame), `Sheet.tsx` (`HealthSheet` + `HealthField`), `sports.ts` (registry SPORTS на все 21 sport_kind с accent/icon/labelRu, helpers `sportLabel/sportAccent/sportIcon/muscleGroupLabel/formatPace/formatDuration/formatVolume`), `WorkoutCard.tsx`, `SetRow.tsx` (inline edit reps/weight/RPE), `RestTimer.tsx` (читает store, тикает 250ms, auto-show при rest_timer). 6 экранов в `screens/health/`: [WorkoutsScreen.tsx](second-brain/telegram-miniapp/src/screens/health/WorkoutsScreen.tsx) (`/health/workouts` — список + sport-фильтр + active-session banner), [NewWorkoutScreen.tsx](second-brain/telegram-miniapp/src/screens/health/NewWorkoutScreen.tsx) (`/health/workouts/new` — 3×4 sport picker + title → создаёт сессию → редирект в Live), [WorkoutDetailScreen.tsx](second-brain/telegram-miniapp/src/screens/health/WorkoutDetailScreen.tsx) (`/health/workouts/:workoutId` — сводка + группировка sets по exercise_id + finish/delete), [LiveSessionScreen.tsx](second-brain/telegram-miniapp/src/screens/health/LiveSessionScreen.tsx) (`/health/workouts/:workoutId/log` — `protectedRouteNoTabBar`, ExercisePicker sheet с поиском, авто-start сессии, авто-копирование предыдущего set, авто-старт rest timer на `exercise.default_rest_seconds`, toggle auto-rest), [ExerciseLibraryScreen.tsx](second-brain/telegram-miniapp/src/screens/health/ExerciseLibraryScreen.tsx) (`/health/exercises` — фильтр по muscle + search), [ExerciseDetailScreen.tsx](second-brain/telegram-miniapp/src/screens/health/ExerciseDetailScreen.tsx) (`/health/exercises/:slug` — GIF + параметры + equipment + secondary muscles + instructions). Routes зарегистрированы в [routes.tsx](second-brain/telegram-miniapp/src/app/routes.tsx). Vitest тесты: [active-workout-store.test.ts](second-brain/telegram-miniapp/tests/active-workout-store.test.ts) (start/stop/restTimer/restTimerRemaining), [workouts-list.test.tsx](second-brain/telegram-miniapp/tests/workouts-list.test.tsx) (empty state + list rendering + active banner) — 10 кейсов, все 66 тестов в suite зелёные. План: [splendid-roaming-chipmunk.md](/.claude/plans/splendid-roaming-chipmunk.md), Spec Kit: `.specify/specs/009-workouts/`. Phase 3+ (multi-sport metrics views, progress/1RM/recovery/programs/AI program/parser/Body Battery) пока не реализованы.
- Health Hi-fi редизайн (mini-app, green/white по дизайн-handoff): [HealthScreen.tsx](second-brain/telegram-miniapp/src/screens/health/HealthScreen.tsx) + [health.css](second-brain/telegram-miniapp/src/screens/health/health.css) под скоупом `.health-app` (Inter Tight + JetBrains Mono, основной `#1F9D6B`, глубокий `#0F7A52`, мятный `#E9F5EE`, screen-bg `#F4F8F5`). Секции: header (дата + «Здоровье»), Dashboard hero (status-pill по readiness, таб 7d/30d/90d управляет `getHealthDashboard({days})`, большой score-ring с делениями и градиентом, 3 mini-метрики Сон/Готовность/HRV), Activity rings (Движение/Упражнения/Стояние из `latest_activity.calories|active_minutes|stand_hours`), Quick state (3 шкалы 1–10 → `POST /health/daily`), Sleep input (Длительность ±15/30мин + Качество 1–10 → `POST /health/sleep`), AI insights (severity → паттерн/внимание/важно), 6 Stage-04 модулей (Сон + 7-day bar, Активность + bar, Тренировки + list, Питание + макросы, Биомаркеры + 3×2 grid, Медкарта). Backend API уже подключён ([api/health.py](second-brain/backend/api/health.py)) — 16 endpoint'ов, `/health/dashboard|daily|sleep|activity|workouts|nutrition|biomarkers|medical-records`, требует миграцию `019_health.sql` в Supabase. Тест [tests/health.test.tsx](second-brain/telegram-miniapp/tests/health.test.tsx) обновлён под новый header.
- Profile Hi-fi редизайн (mini-app, cream/forest-green по дизайн-handoff): [ProfileScreen.tsx](second-brain/telegram-miniapp/src/screens/profile/ProfileScreen.tsx) + [profile.css](second-brain/telegram-miniapp/src/screens/profile/profile.css) под скоупом `.profile-app` (cream `#F2ECDE` + forest-green `#1F5240` + navy-deep `#0F172A` для AI-memory карточки). Секции: header, Hero (аватар с прогресс-кольцом, имя из `getMe().profile.name`, бар «Профиль заполнен» — `profileCompletion()` по 6 полям профиля), AI-память (тёмная карточка с LIVE-индикатором, превью первой записи из `getMemoryProfile()`, 3 stat-блока — записей памяти / задач с целью / задач всего), Поведение ассистента (Стиль/Частота/Глубина — локальный state без backend persist), Безопасность и данные (Локально + AES-256 badges + 5 toggle-доступов AI по сферам + ссылка privacy), Аккаунт-список (Личные данные / Подписка с PLUS-badge из `getPremiumStatus()` / Поддержка / Terms), Footer (Sign out + Delete account). Блок «Сферы жизни» убран — дублировал плитки `/today`. Тест [tests/profile.test.tsx](second-brain/telegram-miniapp/tests/profile.test.tsx) — 3 кейса (профиль + удаление, support, pending deletion).
- Dev-auth: `POST /telegram/auth/dev-session` (включается `TELEGRAM_DEV_AUTH_ENABLED=true` в `.env`) выдаёт JWT для локальной разработки без Telegram initData. LaunchScreen в `import.meta.env.DEV` сам дёргает endpoint; в проде endpoint возвращает 404. Минимум обязательных env для dev: `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`, `SUPABASE_JWT_SECRET`, `APP_SESSION_JWT_SECRET`, `TELEGRAM_DEV_AUTH_ENABLED=true`; AI/payment ключи (`ANTHROPIC_API_KEY`, `REVENUECAT_WEBHOOK_SECRET`, `ADMIN_CLEANUP_SECRET`) опциональны в dev (default пустые), но обязательны в проде.
- Backend `config.py` ищет `.env` рекурсивно вверх от `second-brain/backend/` до корня репозитория — можно держать единый `/AI-asistent/.env` для всех компонентов; локальный `backend/.env` (если есть) переопределяет корневой через `os.environ.setdefault`.
- `docs/legal/` — privacy policy + terms (RU/EN)
- `docs/legal-site/` — Next.js сайт, рендерит `docs/legal/*.md` на `https://second-brain.app/privacy|/terms`
- `docs/store-listing/` — App Store / Play Console тексты + placeholder-скриншоты
- `docs/runbooks/` — operational runbooks (`account-cleanup.md`)
- `docs/superpowers/plans/` — Superpowers-style планы (реализация + reliability)
- `.specify/` — Spec Kit artefacts (gitignored, локально)
  - `memory/constitution.md` — v1.0.0, 7 принципов, обязательны для всех новых фич
  - `specs/001-goals-first-class/` — Goals
  - `specs/002-evening-reflection/` — Reflection
  - `specs/003-oauth-signin/` — OAuth
  - `specs/004-paywall-premium/` — Paywall
  - `specs/005-production-readiness/` — Production readiness (account deletion, history cutoff, i18n, RLS tests, store listing)
  - `specs/008-email-auth/` — Email magic-link auth + связка с Telegram (план, ещё не реализовано; ждёт `RESEND_API_KEY` + DNS для `second-brain.app`)

## Команды

### Backend
- Запуск: `cd second-brain/backend && uvicorn main:app --reload`
- Тесты: `cd second-brain/backend && pytest`
- Линтер: `cd second-brain/backend && ruff check .`

### Mobile
- Запуск: `cd second-brain/mobile && npx expo start --dev-client` (dev-build required из-за нативных модулей)
- Type check: `cd second-brain/mobile && npx tsc --noEmit`
- Тесты: `cd second-brain/mobile && npx jest`
- EAS билд: `cd second-brain/mobile && eas build --profile development --platform all`

## Соглашения
- **Новые фичи — через Spec Kit flow**: `/speckit-specify` → `/speckit-plan` (Constitution Check обязателен) → `/speckit-tasks` → `/speckit-implement`
- **Constitution gates** в `.specify/memory/constitution.md` — приоритет Principle VI: полный функционал, никаких MVP shortcuts
- **Backend:** все user-owned таблицы с RLS; `.env` никогда не коммитить; missing env var → падение при старте (Principle IV)
- **Mobile:** expo-audio (не expo-av); Zustand + MMKV для персистентности; Expo Router v4
- **AI-вызовы:** всегда через `services/ai_router.py` с per-user token budget gating (Principle III). `services/parser.py` имеет локальный `_fallback_parse_dump` — если LLM-tier'ы не вернули валидный JSON, taski всё равно создаются локально (помечаются заметкой), а не выкидывается ошибка.
- **Premium gating:** fail-safe to FREE если `user_premium` row отсутствует — никогда не давать premium по ошибке

## Переменные окружения
- `second-brain/backend/.env.example` — backend template
- `second-brain/mobile/.env.example` — mobile template
- Railway Variables — серверные значения (`SUPABASE_*`, `GROQ_API_KEY`, `ANTHROPIC_API_KEY`, `OPENAI_API_KEY`, `SENTRY_DSN`, `REVENUECAT_WEBHOOK_SECRET`, `DAILY_FREE_TOKEN_BUDGET`, `DAILY_PREMIUM_TOKEN_BUDGET`)
- EAS secrets — mobile build-time (`EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID`, `EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID`, `EXPO_PUBLIC_REVENUECAT_IOS_API_KEY`, `EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY`)

## Release checklist
- `second-brain/RELEASE_CHECKLIST.md` — 12 шагов до прода (обновлён в 005-production-readiness)
- OAuth активация: `second-brain/docs/oauth-setup.md`
- RevenueCat активация: `second-brain/docs/revenuecat-setup.md`
- Cleanup-cron runbook: `docs/runbooks/account-cleanup.md`
- Legal site deploy: `docs/legal-site/README.md`

## Admin / automation
- `DELETE /auth/account` — soft-delete, grace 30 дней. Тесты: `backend/tests/test_account_deletion.py`.
- `POST /admin/cleanup-deleted` — hard-delete по истечении grace. Secret: `ADMIN_CLEANUP_SECRET`. Cron: `.github/workflows/cleanup-cron.yml` (ежедневно 04:15 UTC).
- RLS integration suite: `backend/tests/test_supabase_rls.py` (marker: `integration`). Запуск вручную через `.github/workflows/rls-integration.yml`.
